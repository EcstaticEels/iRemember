import React from 'react';

var ReminderList = (props) => (
  <div className="reminder-list">
  
  </div>
);

module.exports = ReminderList;

// {Array.isArray(props.) ? props.videos.map((item, index) => (
//     <VideoListEntry clickHandler={props.clickHandler} video={item} key={index} />
//   )) : <VideoListEntry clickHandler={props.clickHandler} video={props.videos} key={0} />
//   }